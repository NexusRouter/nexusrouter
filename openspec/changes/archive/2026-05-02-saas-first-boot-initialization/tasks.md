## 1. 数据层与领域模型

- [x] 1.1 新增 `system_bootstrap`（或设计已定表名）迁移：含 `initialized`、`init_in_progress`（或等价）、`lock_version`、`updated_at` 等字段及默认单行种子
- [x] 1.2 定义 GORM 模型与 `BootstrapRepository`：状态读取、`SELECT FOR UPDATE` 互斥、事务封装完成初始化与重置
- [x] 1.3 超管账户写入与密码散列（Argon2id/bcrypt）集成到初始化事务内

## 2. 网关 HTTP 与中间件

- [x] 2.1 实现 `BootstrapGate` 中间件：读取初始化标志（带短 TTL 缓存可选）、白名单路径匹配、未初始化非白名单返回统一 JSON 错误（稳定 `code`）
- [x] 2.2 注册路由：`GET` 状态、`POST` 完成初始化、`POST` 重置（超管鉴权）；在 Wire 中注入依赖
- [x] 2.3 完成初始化 handler：校验 payload、开启事务、处理进行中标记、冲突与「已锁定」分支及 Zap 审计日志
- [x] 2.4 重置 handler：绑定现有 JWT/会话与超管角色校验；成功后清理必要会话或文档化限制

## 3. 测试与契约

- [x] 3.1 为仓库层与 handler 添加表驱动测试：一次性成功、重复完成、双并发互斥、门闸白名单
- [x] 3.2 更新或新增 Swag 注释/OpenAPI 片段，使路径与错误 `code` 与 spec 一致

## 4. 仪表盘前端

- [x] 4.1 新增 `services/bootstrap.ts`（或等价）：封装状态查询与提交 API（axios 基址与错误解析复用现有模式）
- [x] 4.2 实现 `/setup` 向导页：表单（超管账号、基础配置最小字段）、提交反馈、初始化中轮询状态
- [x] 4.3 根路由守卫：未初始化重定向至 `/setup`，已初始化访问 `/setup` 重定向至登录路由
- [x] 4.4 前端单元/组件测试或 Storybook 快照（可选其一，与项目现有测试策略对齐）

## 5. 收尾

- [x] 5.1 本地端到端验证：空库启动 → 向导 → 登录 → 重置 → 再次向导
- [x] 5.2 在 `README` 或运维小节增加「首次部署与重置」说明（若仓库要求文档同步）

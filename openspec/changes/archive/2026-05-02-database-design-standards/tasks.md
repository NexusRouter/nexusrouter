## 1. Validation and review

- [x] 1.1 Run `openspec validate database-design-standards --strict` and fix any reported issues
- [x] 1.2 Peer-review `proposal.md`, `design.md`, and `specs/database-design-standards/spec.md` for alignment with team persistence plans

## 2. Repository adoption

- [x] 2.1 Add a one-line pointer in the appropriate contributor or architecture doc (only if the team maintains such a doc) linking reviewers to `openspec/specs/database-design-standards/` after archive
- [x] 2.2 For the first PR that introduces relational tables, explicitly checklist requirements from the archived spec in the PR description

## 3. Close-out

- [x] 3.1 After merge, run `/opsx:apply` follow-through or `openspec archive` per project workflow to promote `specs/database-design-standards/spec.md` into `openspec/specs/`

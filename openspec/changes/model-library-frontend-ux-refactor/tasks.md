## 1. 文案与 i18n 基线

- [ ] 1.1 梳理 `pages.modelLibrary` 中英文键：主标签、向导步骤、术语 glossary、空态与阻断提示
- [ ] 1.2 新增 `TermHint`（或复用 Tooltip + 图标）组件，绑定 glossary 键

## 2. 信息架构与路由

- [ ] 2.1 调整 `ModelLibrary.tsx`（或拆分子组件）：默认渲染实例列表为主视图，四表视图收拢为「高级维护」
- [ ] 2.2 实例列表列定义：聚合展示 `model_code`、展示名、厂商、上游摘要、`provider_model_code`、状态与操作列

## 3. 向导模式

- [ ] 3.1 实现向导容器（步骤条 + 每步表单），顺序：厂商 → 上游 → 逻辑模型 → 实例
- [ ] 3.2 每步对接现有 POST/PUT API，成功后再进入下一步；失败时用 message 展示错误
- [ ] 3.3 完成最后一步后跳转或刷新实例列表并关闭向导

## 4. 下拉内嵌新建

- [ ] 4.1 抽象可复用 `SelectWithCreate`：接收 `onCreate` 打开子 Modal、刷新 query、回写选中值
- [ ] 4.2 在实例表单中为逻辑模型、厂商、上游三处接入（写权限可见）
- [ ] 4.3 子 Modal 复用现有字段校验逻辑，避免重复请求体拼装

## 5. 空态与只读

- [ ] 5.1 实例/各实体列表空态：引导文案 + 进入向导或新建（仅写角色）
- [ ] 5.2 回归 `readOnly`（操作员）：隐藏主 CTA、行内编辑、内嵌新建

## 6. 专家模式与回归

- [ ] 6.1 「高级维护」中保留四实体表格与原有 `EditModal` 行为，确保与 API 一致
- [ ] 6.2 手动回归：创建全链路、编辑、删除、上游同步入口、中英文切换
- [ ] 6.3 运行 `pnpm exec tsc --noEmit` 与 `pnpm lint`（于 `web/dashboard`）

schema: spec-driven

# Shown to AI when creating OpenSpec artifacts. Full narrative: openspec/project.md
context: |
  NexusRouter: open-source LLM API gateway (OpenAI-compatible), AGPL-3.0.
  Backend: Go 1.24.x, Gin — module root at services/gateway/ (see go.mod), entry cmd/api.
  Frontend: pnpm + Vite + React in web/dashboard/.
  Specs: openspec/specs/; active proposals: openspec/changes/.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

# https://golangci-lint.run/usage/configuration/
version: "2"

run:
  timeout: 5m

linters:
  default: standard
  settings:
    errcheck:
      check-type-assertions: true

issues:
  max-same-issues: 0
